- name: Cleanup untagged container images yolks
  uses: mitchfen/cleanup-untagged-container-images@v1.0.2
  with:
    username: '4fx'
    container-name: 'yolks'
    token: ${{ secrets.GITHUB_TOKEN }}
